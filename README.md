این پروژه یک اسکریپت پایتون سبک، سریع و سازگار با Python 3.13 است که فایل‌های XML را بدون خراب کردن ساختار، به‌صورت کامل به زبان فارسی ترجمه می‌کند.
اسکریپت از کتابخانه‌ی deep-translator استفاده می‌کند و با روش Batch Translation سرعت ترجمه را چند برابر افزایش می‌دهد. همچنین دارای Progress Bar و گزارش نهایی است.

✨ ویژگی‌ها

✅ ترجمه کامل فایل XML بدون تغییر در ساختار (Structure Safe)

🚀 سرعت بسیار بالا با استفاده از Batch Translation

📊 نمایش درصد پیشرفت ترجمه (Progress Bar – tqdm)

📑 گزارش نهایی شامل:

تعداد کل آیتم‌ها

تعداد ترجمه‌های موفق

تعداد خطاها

🎯 پشتیبانی از متن‌ها و Attribute ها

💯 سازگار با Python 3.10 → 3.13

🔁 کاملاً آفلاین نیست اما از API رایگان گوگل (غیررسمی) استفاده می‌کند

🧩 پیش‌نیازها

نصب پایتون:

Python 3.10 ، 3.11 ، 3.12 یا 3.13
(توصیه: Python 3.11 یا 3.12)

کتابخانه‌های مورد نیاز:

pip install deep-translator tqdm

📥 نصب پروژه
1. کلون کردن از GitHub:
git clone https://github.com/username/xml-translator.git

2. ورود به پوشه پروژه:
cd xml-translator

3. نصب وابستگی‌ها:
pip install -r requirements.txt


فایل requirements.txt باید شامل:

deep-translator
tqdm

▶️ اجرای اسکریپت
python translate_xml.py input.xml


مثال:

python translate_xml.py strings.xml


خروجی با نام زیر ذخیره می‌شود:

strings_fa.xml

🔧 نحوه عملکرد اسکریپت
1. استخراج همه‌ی متن‌ها

اسکریپت به‌صورت بازگشتی تمام Nodeها و Attributeهای فایل XML را پیدا می‌کند.

2. تجمیع در یک لیست

به‌جای ارسال تک‌به‌تک، همه متن‌ها در لیست جمع می‌شوند (برای افزایش سرعت).

3. پردازش دسته‌ای

متن‌ها در بسته‌های ۵۰تایی به Google Translate ارسال می‌شوند:

translate_batch()


این روش سرعت را تا ۱۰ برابر افزایش می‌دهد.

4. نمایش درصد پیشرفت

با tqdm برای هر بسته یک Progress Bar نمایش داده می‌شود.

5. جایگزینی ترجمه‌ها

متن‌ها با نسخه ترجمه‌شده جایگزین می‌شوند.

6. ذخیره فایل

خروجی با ساختار اصلی XML ذخیره می‌شود.

🛑 محدودیت‌ها

این پروژه از Google Translate غیررسمی استفاده می‌کند
→ اگر تعداد ترجمه بیش از حد شود ممکن است گوگل موقتاً بلاک کند.

اگر فایل بسیار بزرگ باشد بهتر است:

Batch Size را کمتر کنید

یا ترجمه را در چند مرحله انجام دهید

ساختار XML حفظ می‌شود اما:

اگر فایل دارای CDATA پیچیده باشد، ممکن است نیاز به اصلاح داشته باشد (نادر)

🗂 ساختار فایل‌ها
xml-translator/
│
├── translate_xml.py   ← اسکریپت اصلی
├── requirements.txt
└── README.md

⚙️ کد اسکریپت (برای README)

اگر خواستی، می‌تونی این کد رو به‌صورت کامل در بخش "Source Code" قرار بدی 👇
(این همان نسخه‌ای است که برایت نوشتم؛ در صورت نیاز دوباره هم می‌فرستم)

📜 لایسنس پیشنهادی

برای پروژه‌های اسکریپتی مثل این، MIT License بهترین انتخاب است:

MIT License
Copyright (c) 2025

Permission is hereby granted, free of charge, to any person obtaining a copy...


(مایل باشی برات فایل LICENSE هم آماده می‌کنم)

🧪 نمونه خروجی اجرای برنامه
🔄 در حال ترجمه 120 بخش...
در حال ترجمه: 45%|███████████▋           | 54/120 [00:10<00:09,  6.5بخش/s]

📊 گزارش نهایی:
✅ کل بخش‌ها: 120
✅ ترجمه موفق: 118
⚠️ خطاها: 2
📂 فایل خروجی: strings_fa.xml
